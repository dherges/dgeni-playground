
/** A foo class doing bar things. */
export class Foo {

  /**
   * Does things.
   *
   * @param bar A bar string
   * @returns A magic number
   */
  public foo(bar: string): number {
    return 7;
  }
}
